[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Unknown paths render the SPA, which shows the caveman 404 page.
# /api/leetcode is served by netlify/functions/leetcode.ts (config.path).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
